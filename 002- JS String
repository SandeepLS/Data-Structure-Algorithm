JS String:- are used for, Storing and Manipulating text. It can contains zero or more character within quotes.
            They are two way to create string:= By string literal, By string object.

Ex:-// string written in inside a quotes.
var x = "Welcome to India";
console.log(x);

//Declear an object
var x = new String("Welcome to IND");
console.log(x);

//Finding the 1st char of "e"
var x = "Welcome";
console.log(x.indexOf('e'));

//Replace the word
let a = "Hello World";
let arrString = ["India", "In", "Worls"];
console.log(a.replace("World", arrString[0]));

